import type { Session } from '@supabase/supabase-js'

/**
 * 최초 사용 튜토리얼은 각 실제 화면 위에서 동작하는 체험형 안내로 전환했습니다.
 * 현재 1단계는 홈 Hero 카드가 직접 안내를 담당합니다.
 */
export function FirstUseTutorial(_props: { session: Session | null }) {
  return null
}
